BITS 16
ORG 0x7C00
START:
	XOR EAX,EAX
	MOV DS,AX
	MOV SS,AX
	MOV SP,0x7C00
	MOV ES,AX
	MOV AH,0x42
	MOV DL,0x80
	MOV SI,DAP
	INT 0x13
	JC HALT
	JMP 0x1000
HALT:
	HLT
	JMP HALT

DAP:
	DB 16,0
	DW 0x02
	DW 0x1000
	DW 0
	DD 0x00000001
	DD 0x00000000
	
times 510-($-$$) DB 0
DW 0xAA55
